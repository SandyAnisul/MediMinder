// Admin dashboard shell (nav, auth guard). Built in Part D Step 3.
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-screen">{children}</div>;
}
